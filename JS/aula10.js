let num = 10

res = (num%2 ? "Par" : "Impar")

console.log(res)

res = (!(num%2) ? "Par" : "Impar")

console.log(res)

let n1 = 10
let n2 = 5

res = (n1 > n2 ? "Verdadeiro" : "Falso")

console.log(res)

let st = "A"

res = (st == "A" ? "Ativo" : "Inativo")

console.log(res)